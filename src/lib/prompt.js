export const requestChurchFathers = (verse) => {
  console.log(verse);
  return `what did the church fathers thought about ${verse}. respond list of church fathers commentary, including reference in json format 
`;
};
